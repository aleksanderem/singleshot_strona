node_modules/
.next/
.astro/
dist/
build/
*.log
.DS_Store
.env
.env.local
.env.*.local
*.tsbuildinfo
next-env.d.ts

# dev-only symlinks to videos/uploads (Astro pulls media from Payload at build)
web/public/videos
web/public/uploads

# don't commit binary media — production uses Payload uploads (filesystem in dev, S3 in prod)
videos/
uploads/
cms/media/

# leftovers from earlier zip extract on prod (safe to ignore)
__MACOSX/
*.zip
